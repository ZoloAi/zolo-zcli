# zMocks/zUI.zNavigation_test_target.yaml
# Mock target file for testing inter-file zLink navigation

zVaF:
  # Target block for inter-file navigation
  Target_Block:
    zDisplay:
      event: text
      content: "Target File - Inter-file navigation successful!"
    
    zMenu:
      options:
        - "Return"
  
  # Another block with permission requirements
  Admin_Block:
    zDisplay:
      event: text
      content: "Admin Block - RBAC navigation successful!"
    
    zMenu:
      options:
        - "Return"
  
  # Block with nested navigation
  Nested_Block:
    zDisplay:
      event: text
      content: "Nested Block - Complex navigation successful!"
    
    zMenu:
      options:
        - "Go deeper"
        - "Return"
  
  Deep_Block:
    zDisplay:
      event: text
      content: "Deep Block - Multi-level navigation successful!"
    
    zMenu:
      options:
        - "Return"


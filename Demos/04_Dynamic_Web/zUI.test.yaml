# Demo 4.2 - zBifrost zDisplay Event Tests
# Tests header, text, success, info, and JSON output events

zVaF:
  ~Root*: ["^TestHeader", "^TestText", "^TestSuccess", "^TestInfo", "^TestJSON", "stop"]
  
  "^TestHeader":
    zDisplay:
      event: header
      label: "📋 This is a Header Event"
  
  "^TestText":
    zDisplay:
      event: text
      content: "This is a simple text display event. It can contain multiple lines of text and will be rendered as plain paragraphs in both Terminal and Web modes."
  
  "^TestSuccess":
    zDisplay:
      event: success
      content: "Operation completed successfully! ✅"
  
  "^TestInfo":
    zDisplay:
      event: info
      content: "ℹ️  This is an informational message. Use it to provide context or guidance."
  
  "^TestJSON":
    zDisplay:
      event: json_data
      data:
        message: "This is JSON output"
        timestamp: "2024-11-09"
        status: "success"
        items: ["item1", "item2", "item3"]
